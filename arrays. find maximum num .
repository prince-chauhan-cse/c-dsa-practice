#include <stdio.h>
int main(){
int a[10],max,i;
// Arrays, enter the number for the store value in the array.

printf("Enter the TEN number :");
for(i=0;i<10;i++){
scanf("%d",&a[i]);
}
max=a[0];
for(i=1;i<10;i++){
if(max < a[i]){
max=a[i];}
}
printf(" %d THE MAXIMUM NUMBER IS : ",max);
return 0;
}
